name: 'dbt_transformation_layer'
version: '1.0.0'

profile: 'dbt_transformation_layer'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_transformation_layer:

    silver:
      +materialized: table
      +transient: false

    gold:
      +schema: "GOLD"
      +materialized: view